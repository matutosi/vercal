# vercal プロジェクト

週間の縦型カレンダー (vertical calendar) を作成する Python のコード．
Streamlit の web 版と，手元で実行する版がある．

公開先: <https://vercal.streamlit.app/>

## 主なファイル

- `vercal.py` … カレンダー生成の本体
- `vercal_web.py` … Streamlit の web 版
- `event.py` … 予定 (繰り返しを含む) の扱い
- `schedule.xlsx` … 繰り返しの予定を入力するエクセルの雛形
- `requirements.txt` … 依存パッケージ (streamlit の最小版を指定してある)
- `HackGen35Console-Regular.ttf` … 描画に使うフォント
- `img/` … README 用の画像

## 決めごと

- 設定は web 版の左サイドバーに集約する
  (年，4月始まり/1月始まり，1日の開始・終了時刻，月曜始まり/日曜始まり，左寄せ/右寄せ)．
- **年の既定値は「今の月から決める」** (年度末に翌年を出す)．過去に2回直している箇所なので注意する．
- 繰り返しの予定はエクセルのアップロードで受け取る．書式を変えるときは README の説明も直す．

## 進捗状況

### 現在の状態

- 2026-08-28 12:05
  **リファクタ A〜G をすべて実施した** (実行部を argparse の CLI に，未使用引数・
  マジックナンバー・可変既定引数の整理，ベクトル化)．**web 版のボタンが前回の設定で
  作っていたバグ**も直した (AppTest で再現・検証)．テストは 50 件．

- 2026-08-28 11:55
  **単発の予定 (曜日が空) と，長い予定名の折り返しに対応した** (ユーザ判断: 開始日の1日だけ)．
  時刻も空の予定は終日扱いでメモ欄に描く．**同梱の `schedule.xlsx` がそのまま通るようになった**．
  テストは 43 件 (`pytest`)．残るはリファクタ候補の検討だけ．

- 2026-08-28 11:40
  **バグを6件見つけて5件を直し，pytest のテスト 33 件を追加した** (`tests/`)．
  エクセルの日付・時刻セル，終了時刻なし，予定なしの生成，時間帯の逆転で落ちていた．
  残る1件 (曜日が空の単発予定) は仕様の判断待ち (下記)．

- 2026-08-28 11:15
  **同梱フォントのライセンス表記を整えた** (`LICENSE_HackGen.txt` を追加，README に「License ライセンス」節)．
  **README に実際の出力例を追加した** (`img/vercal_output_example.png`)．
  出力例を作る過程で，未報告のバグを2件見つけた (下の「次にやること」)．

- 2026-08-20 08:39
  プロジェクト管理用の `.claude/CLAUDE.md` を新規に設置した．
  実装の最終更新は 2026-03-06 で，年の既定値の算出と streamlit の最小版指定までが入っている．

### 次にやること

- ~~実行例の gif か png を README に追加する~~ **2026-08-28 完了** (`img/vercal_output_example.png`)．
- ~~フォントを同梱しているので，配布時のライセンス表記を確認する~~ **2026-08-28 完了**．
  白源 (HackGen) は **SIL OFL 1.1**，Reserved Font Name は「白源」「HackGen」．
  無改変で再配布しているので，全文 (`LICENSE_HackGen.txt`) を同梱すれば足りる．
  `.gitignore` の `*.ttf` に例外 (`!HackGen35Console-Regular.ttf`) を入れた
  (追跡済みなので消えはしないが，取り違え防止)．
- ~~【判断待ち】曜日が空の行 (単発の予定) の扱い~~ **2026-08-28 決定・対応済み**．
  **曜日が空なら `period_start` の1日だけ** (`period_end` は見ない)．
  **開始時刻も空なら終日の予定**として，時間軸ではなく**メモ欄**に描く．
- ~~【未修正】予定名が長いと隣の日にはみ出す~~ **2026-08-28 対応済み** (`wrap_text` で折り返し，
  枠の高さを超える分は `...` で打ち切り)．
- ~~【リファクタ候補】~~ **2026-08-28 に A〜G をすべて実施** (ユーザ判断)．
  - CLI (`python vercal.py --year 2026 --schedule schedule.xlsx`) を用意した．
    `--font` を省くと同梱フォントを使う (以前の既定は Windows の CENTURY.ttf だった)．
  - **web 版のボタンはコールバックをやめた**．`on_click` だと**その回の設定が代入される前**に
    走るため，前回の設定でカレンダーが作られていた (直す前の版で再現を確認)．
    ボタンの位置は `st.empty()` のプレースホルダで従来どおり上に置いている．
  - `add_page`・`add_draw_year_month` は `shift`+`cumsum` にした (添字に依存しない)．
- **テストは `pytest` で 50 件** (`tests/`)．web 版は Streamlit の `AppTest` で動かしている．
